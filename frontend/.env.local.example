# Local dev: point at your Flask backend
NEXT_PUBLIC_API_URL=http://localhost:5000

# Production (Vercel): set this in Project Settings > Environment Variables
# to your deployed Render backend URL, e.g.
# NEXT_PUBLIC_API_URL=https://resume-match-backend.onrender.com
